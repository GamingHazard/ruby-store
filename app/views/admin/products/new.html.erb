<% content_for :title, "New product" %>

<div class="flex flex-col items-center justify-center flex-1 w-full h-screen bg-gray-100">
  <h1 class="mb-10 text-4xl font-bold">New product</h1>

<span class = "flex flex-row items-center justify-evenly">
    <%= render "form", product: @product %>

</span>
</div>
